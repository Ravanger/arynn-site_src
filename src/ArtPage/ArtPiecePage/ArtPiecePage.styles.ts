import styled from "styled-components"

export const MainArtPiecePage = styled.main`
  width: 100%;

  > div {
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    align-items: center;

    > div {
      width: 100%;
    }
  }
`
