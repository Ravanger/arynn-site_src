import styled from "styled-components"

export const SpanSpacer = styled.span`
  display: block;
  width: var(--spacerWidth, 1px);
  min-width: var(--spacerWidth, 1px);
  height: var(--spacerHeight, 1px);
  min-height: var(--spacerHeight, 1px);
`
